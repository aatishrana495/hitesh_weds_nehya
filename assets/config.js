/* Public configuration only. Never put API secrets in this browser-delivered file. */
window.WEDDING_CONFIG = Object.freeze({
  // Preferred: an HTTPS endpoint accepting a JSON POST and returning a 2xx response.
  rsvpEndpoint: "",
  // Fallback: digits only, including country code. Example: "919999999999".
  whatsappNumber: "",
  venueSummary: "Hyderabad, India"
});
